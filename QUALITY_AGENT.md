# QUALITY_AGENT.md

This project quality workflow now lives as the Codex skill `$quality-agent`.

Skill location:

```text
.codex/skills/quality-agent/SKILL.md
```

When asked to run QA, verify linters, check project quality, or validate CI readiness, use `$quality-agent`.
