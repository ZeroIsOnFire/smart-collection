<div class="container-fluid py-4 app-container">
  <%= render 'form', wishlist_item: @wishlist_item, title: t('wishlist_items.edit.title', name: @wishlist_item.name), subtitle: t('wishlist_items.edit.subtitle') %>
</div>
