<% if current_user.sharing_enabled? %>
  <div class="mb-4">
    <label class="form-label fw-medium small"><%= t('sharing_settings.public_link') %></label>
    <div class="input-group" data-controller="clipboard" data-clipboard-success-content-value="<%= t('sharing_settings.copied') %>">
      <input type="text" class="form-control bg-light border-0 rounded-start-3 ps-3 small" readonly
             value="<%= public_share_url(current_user.share_token, current_locale_param) %>"
             data-clipboard-target="source">
      <button class="btn btn-primary rounded-end-3 px-3 d-flex align-items-center gap-2" type="button" data-action="clipboard#copy" data-clipboard-target="button">
        <i class="bi bi-clipboard"></i>
      </button>
    </div>
  </div>
<% end %>

<%= button_to toggle_sharing_cars_path,
              method: :patch,
              form: { class: "d-block" },
              class: "btn #{current_user.sharing_enabled? ? 'btn-light text-danger' : 'btn-premium'} rounded-3 w-100 py-2 d-flex align-items-center justify-content-center gap-2" do %>
  <% if current_user.sharing_enabled? %>
    <i class="bi bi-x-circle"></i> <span><%= t('sharing_settings.disable') %></span>
  <% else %>
    <i class="bi bi-check-circle"></i> <span><%= t('sharing_settings.enable') %></span>
  <% end %>
<% end %>
